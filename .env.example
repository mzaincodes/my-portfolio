# Optional. When set, contact form submissions are forwarded to this endpoint
# as JSON ({ name, email, subject, message, receivedAt }). Works with Resend,
# Formspree, a Zapier/n8n webhook, or your own API route.
# Without it, submissions are validated and logged server-side.
CONTACT_WEBHOOK_URL=
